<%= form_for(pixel) do |f| %>
  <% if pixel.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pixel.errors.count, "error") %> prohibited this pixel from being saved:</h2>

      <ul>
      <% pixel.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :description %>
    <%= f.text_field :description %>
  </div>

  <div class="field">
    <%= f.label :tags %>
    <%= f.text_field :tags %>
  </div>

  <div class="field">
    <%= f.label :tracks_id %>
    <%= f.text_field :tracks_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
